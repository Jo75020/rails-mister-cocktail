
<%= render "banner" %>
<div class="container">
<div class="padded text-center">What do you want to drink?</div>
  <div class="row">
    <% @cocktails.each do |cocktail| %>
      <div class="col-xs-12 col-sm-4">
      <%= render "card", cocktail: cocktail %>
      </div>
    <% end %>
  </div>
</div>
